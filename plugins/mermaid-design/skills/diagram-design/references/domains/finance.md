# finance

Preserve currencies, units, periods and whether values are stocks or flows. Separate authorization, settlement and reconciliation. Never infer accounting balances, regulatory status or risk ratings from a diagram.

Apply the shared grammar, compatibility and complexity policies; this reference only changes domain modeling.
