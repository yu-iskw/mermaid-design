# software

Distinguish runtime components, deployment units, code modules and owners. Label protocols only when known. Do not equate a process boundary with a trust boundary.

Apply the shared grammar, compatibility and complexity policies; this reference only changes domain modeling.
